{
  "name": "@yggdrasil/tools",
  "description": "belo tools",
  "version": "0.1.3",
  "author": "belo tech team <tech@belo.app>",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.build.json",
    "compile": "swc src --config-file ../.swcrc.json --out-dir dist",
    "lint": "eslint --cache --ext .js,.ts ./src"
  },
  "dependencies": {
    "memoizee": "^0.4.15"
  },
  "devDependencies": {
    "@yggdrasil/development": "*",
    "@types/memoizee": "^0.4.8"
  }
}
